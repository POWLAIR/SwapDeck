{
  "name": "swapdeck",
  "version": "1.0.0",
  "description": "Plateforme de troc de cartes de collection — sans flux monétaire",
  "scripts": {
    "dev": "cd backend && npm run dev",
    "start": "cd backend && npm start",
    "db:migrate": "cd backend && npx prisma migrate dev",
    "db:seed": "cd backend && npx prisma db seed",
    "db:studio": "cd backend && npx prisma studio",
    "db:reset": "cd backend && npx prisma migrate reset --force",
    "test:unit": "cd backend && npm test",
    "test:e2e": "npx playwright test --config=tests/e2e/playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0"
  }
}
